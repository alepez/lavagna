<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      body {
        background: linear-gradient(
          135deg,
          white 0%,
          white 49%,
          black 49%,
          black 51%,
          white 51%,
          white 100%
        );
        background-repeat: repeat;
        background-size: 20px 20px;
      }

      #container {
        flex:1;
        max-width:100%;
        max-height:100%;
      }

      body {
        display: flex;
      }

      html, body {
        position: fixed;
        margin: 0;
        padding: 0;
        width: 100vw;
        height: 100%;
      }
    </style>
  </head>
  <div id="container">
    <canvas id="bevy">
        Javascript and support for canvas is required
    </canvas>
  </div>
  <script type="module">
    import init from './lavagna.js'
    init()
  </script>
</html>
